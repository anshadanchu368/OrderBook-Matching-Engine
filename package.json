{
  "name": "lob-matching-engine",
  "version": "1.0.0",
  "description": "Docker-only Node.js development environment for an in-memory LOB matching engine",
  "type": "module",
  "scripts": {
    "start": "node src/api/server.js",
    "dev": "node --watch src/api/server.js",
    "test": "node --test",
    "bench:record": "node scripts/benchmarking/record.js"
  },
  "devDependencies": {
    "socket.io-client": "^4.8.3",
    "supertest": "^7.2.2",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "amqplib": "^2.0.1",
    "autocannon": "^8.0.0",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "helmet": "^8.2.0",
    "morgan": "^1.11.0",
    "socket.io": "^4.8.3"
  }
}
